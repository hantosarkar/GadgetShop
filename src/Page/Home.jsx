import React from 'react';
import { Outlet } from 'react-router-dom';
import Nav from '../Component/Nav';

const Home = () => {
    return (
        <div>
              <Nav></Nav>
        </div>
    );
};

export default Home;